import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jyeshta.solitaire',
  appName: 'Jyeshta Solitaire',
  webDir: 'dist'
};

export default config;
